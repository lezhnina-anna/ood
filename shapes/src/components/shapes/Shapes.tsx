import * as React from "react";

export class Shapes extends React.Component {
    render(): React.ReactNode {
        return (
            <div>
                hello
            </div>
        )
    }
}